# SQLite version 3.x
#   gem install sqlite3-ruby (not necessary on OS X Leopard)
development:
  adapter: sqlite3
  database: db/development.sqlite3
  pool: 5
  timeout: 5000

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: sqlite3
  database: db/test.sqlite3
  pool: 5
  timeout: 5000

  #production:
    #adapter: mysql
    #ncoding: utf8
    #database: afmove_frgcms
    #username: afmove_admin
    #password: lufury2
    #host: 127.0.0.1
    #socket: /var/lib/mysql/mysql.sock

#production:
  #adapter: sqlite3
  #database: db/development.sqlite3
  #pool: 5
  #timeout: 5000
 
production:
      adapter: mysql
      encoding: utf8
      reconnect: true
      database: frgcms
      host: 127.0.0.1
      pool: 5
      username: afmove_admin
      password: lufury2
      socket: /var/lib/mysql/mysql.sock
      
  