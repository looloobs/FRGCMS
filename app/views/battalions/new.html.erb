<h1>New Battalion/Squadron</h1>

<% form_for(@battalion) do |g| %>
  <%= g.error_messages %>

  <p>
    <%= g.label :unit %><br />
    <%= g.text_field :unit %>
  </p>
  <p>
    <%= g.label :name %><br />
    <%= g.text_field :name %>
  </p>
  <p>
    <%= g.label :installation %><br />
    <%= g.text_field :installation %>
  </p>


<% g.fields_for :companies do |g| %>
  <%= render :partial => 'company', :locals => { :g => g } %>
<% end %>

  <p>
    <%= g.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', battalions_path %>